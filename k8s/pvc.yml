kind: PersistentVolumeClaim
apiVersion: v1
metadata: 
  name: mysql-pvc
  namespace: notes

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
  selector:
    matchLabels:
      app: mysql
