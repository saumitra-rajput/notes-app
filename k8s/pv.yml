apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv
  namespace: notes
  labels:
    app: mysql

spec:
  storageClassName: manual
  capacity:
    storage: 3Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/mysql

